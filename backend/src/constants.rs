//! Constants used throughout the backend.
// TODO: Remove this. Use error enums instead (or just the normal error).

/// ok
pub const MESSAGE_OK: &str = "ok";
/// A message signalling that the some data could not be inserted in the database.
pub const MESSAGE_CAN_NOT_INSERT_DATA: &str = "Cannot insert data";
/// An empty string
pub const EMPTY: &str = "";
