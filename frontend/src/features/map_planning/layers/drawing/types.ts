export type RectangleProperties = {
  width: number;
  height: number;
};

export type EllipseProperties = {
  radiusX: number;
  radiusY: number;
};

export type FreeLineProperties = {
  points: number[][];
};
