import { DrawingDto } from '../types';

//TODO #1123 - implement createDrawing funtion to call backend
export const createDrawing = async (mapId: number, drawing: DrawingDto): Promise<DrawingDto> => {
  return drawing;
};
